# Docker Compose configuration for FaceValue

services:
  web:
    build: .
    entrypoint: ["/app/entrypoint.sh"]
    command: ["gunicorn", "facevalue.wsgi:application", "--bind", "0.0.0.0:8000"]
    ports:
      - "80:8000"
